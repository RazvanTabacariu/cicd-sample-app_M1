node {
    stage('Preparation') {
        
            sh 'docker stop samplerunning'
            sh 'docker rm samplerunning'
        
    }
    stage('Build') {
        build 'SampleApp'
    }
    stage('Results') {
        build 'SampleAppCURL'
    }
}
